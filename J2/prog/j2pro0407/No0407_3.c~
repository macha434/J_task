#include<stdio.h>

int main(void)
{
  int a,b = 0,c = 0;

  printf("繰り返したい回数を入力してね。/n");
  scanf("%d",&a);

  while(b<=a){

    while(c<=a){

      printf("*\n");

      c = c + 1;
    }

    b = b + 1;
  }

  return 0;
}
