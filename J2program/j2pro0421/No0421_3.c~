#include<stdio.h>
#include<math.h>

int main(void)
{
  int i = 0, num, a;

  printf("Your Number :");
  scanf("%d",&num);

  while(i<=num){
    a = pow(2, i);
    printf("i=%2d: %4d\n",i,a);
    i++;
  }

  return 0;
}
