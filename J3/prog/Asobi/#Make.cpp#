#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
int main()
{
  string date;
  cin>>date;
  string mkdir="mkdir "+date;
  system(mkdir);
  FILE *fp;
  fp=fopen("Makefile","w");
  if(fp==NULL){
    cout<<"Err0r"<<endl;
  }else{
    
  }
}
