#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
int main()
{
  string s;
  cin>>s;
  string t;
  int nums[10]={0};
  for(ll i=0;i<4;i++){
    nums[(int)s[i]-'0']++;
  }
  sort(nums,nums+10);
  if(nums[0]>2) cout<<"Yes"<<endl;
  cout<<"No"<<endl;
  //for(ll i=0;i<=9)
}
